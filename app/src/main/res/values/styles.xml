<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="fontFamily">@font/simonetta</item>
        <item name="android:windowAnimationStyle">@style/CustomAnimation</item>
    </style>

    <style name="exampleToast">
        <item name="colorBackground">@android:color/holo_blue_bright</item>
        <item name="length">SHORT</item>
        <item name="strokeColor">@android:color/holo_red_dark</item>
        <item name="strokeWidth">1dp</item>
        <item name="iconRight">@drawable/ic_android</item>
        <item name="textColor">@android:color/darker_gray</item>
    </style>

    <style name="CustomAnimation" parent="android:Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left</item>
    </style>

</resources>